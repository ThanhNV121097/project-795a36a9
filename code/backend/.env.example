# PostgreSQL connection string injected by runtime or docker compose.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP port for backend server.
PORT=8080

# Fallback HTTP port used when PORT is absent.
APP_PORT=8080
